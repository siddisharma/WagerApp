<%= form_with(model: payout, local: true) do |form| %>
  <% if payout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payout.errors.count, "error") %> prohibited this payout from being saved:</h2>

      <ul>
      <% payout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bet_id %>
    <%= form.number_field :bet_id, id: :payout_bet_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :payout_amount %>
  </div>

  <div class="field">
    <%= form.label :rank %>
    <%= form.number_field :rank, id: :payout_rank %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
